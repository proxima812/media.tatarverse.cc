# <Название задачи>

**Дата:** <YYYY-MM-DD>
**Спека:** `docs/superpowers/specs/<дата>-<тема>-design.md` (если есть; для мелких задач - прочерк)

## Зачем

Что не так сейчас и что должно стать иначе с точки зрения человека,
открывшего сайт. Одним абзацем, без списка технических шагов.

## Что меняем

Список файлов с пометкой, что с каждым происходит.

- Создать: `путь` - зачем
- Изменить: `путь` - что именно
- Удалить: `путь` - почему безопасно

## Чего не трогаем

Явные границы: какие URL, поля фронтматтера, публичные ассеты и
конфигурационные значения остаются как есть. Все, чего нет в разделе
«Что меняем», по умолчанию неприкосновенно, но самое хрупкое стоит
назвать вслух.

## Шаги

- [ ] Шаг 1
- [ ] Шаг 2

## Проверка

Чем убеждаемся, что готово. Минимум - `bun run verify`; сверх того -
что именно посмотреть глазами и на каких страницах (`/`, `/en/`,
`/catalog/<category>`).

## Риски

Что может сломаться незаметно и как это заметить.
