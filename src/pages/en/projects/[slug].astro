---
import ProjectPage from "@/components/pages/ProjectPage.astro";
import { cardPaths } from "@/components/pages/paths";

export const getStaticPaths = () => cardPaths("en");

const { card, related } = Astro.props;
---

<ProjectPage locale="en" card={card} related={related} />
